# Python
__pycache__/
*.pyc
*.pyo

# Local data cache
data_cache/

# ML model binaries
ml_models/

# UI test screenshots (auto-generated, auto-cleaned)
tools/screenshots/
tools/screenshots_*/
test_screenshots/

# 一次性 UI smoke test 腳本
tools/ui_test_*.py
tools/ui_smoke_*.py
tools/*_smoke_test.py
# Playwright UI 驗證腳本（啟 streamlit 截圖到 test_screenshots/；正式單元測試在 tests/ 與 tools/test_{brokerage_yt,notes_view}.py 仍追蹤）
tools/test_macro_ai_report_section.py
tools/test_report_library_scroll_fix.py
tools/test_report_library_ui.py

# 一次性 audit 腳本（驗證後丟，結論寫進 memory）
# ⚠️ 常設工具不要取名 audit_* —— 會被這條規則靜默吃掉，而「檔案在本機、測試也綠」
# 完全看不出來（2026-08-03 踩到：reconcile_panel_vs_official 原名 audit_panel_vs_official，
# commit 只進了測試檔沒進工具，乾淨 clone 上測試會 import 失敗）。
# 常設的對帳／掃描工具用 reconcile_* / scan_* / verify_* 命名。
tools/audit_*.py

# Tools 暫存區（smoke test / batch staging / ad-hoc）
tools/tmp/

# Kronos POC (暫停, memory 有記錄)
tools/kronos_smoke*.py

# Local-only calibration scripts
local/

# Environment
.env
*.env

# IDE
.vscode/
.idea/
.antigravitycli/

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Claude Code local settings (除了 SKILL 與 SUBAGENT 定義檔需版本化追蹤)
# git 規則: 父目錄被排除後子目錄 negation 無效,必須用 .claude/* 方式
.claude/*
!.claude/skills/
!.claude/agents/

# 跨 agent 本機 handoff 檔（AGENTS.md: local handoff files by default，不入版控）
.ai/

# Runtime artifacts
*.stackdump
*.log

# AI 報告輸出（使用者生成，不納入版控）
data/ai_reports/
data/macro_reports/

# 總經報告 JSON：正式輸出在 data/macro_reports/，reports/ 下為非正式位置產物
reports/macro_report_*.json

# Daily-refresh pipeline outputs (每晚重生，untrack 防止 git restore data/ 退回舊版)
# 保留 tracked: strong_stocks_reports/ (per-day archive)、
#               paper_trades/open_trades.json (open state)、sentiment/*history* (累積)
data/sector_tags_dynamic.parquet
data/yt_videos_panel.parquet
data/yt_brokerage_mentions.parquet
data/yt_brokerage_videos.parquet
data/calendar/earnings_call.parquet
data/latest/*.json
data/latest/*.txt
data/news/*.parquet
data/macro/*.parquet
data/breadth/*.parquet

# Macro / banner UI smoke test screenshots（reports/screenshots/banner_regime_ext_* 仍 commit）
reports/screenshots/macro_*
reports/screenshots/banner_full_page_*
reports/screenshots/banner_phase_*
reports/screenshots/brokerage_yt_*

# Root-level scratch (PDF dump etc.)
/temp_*.txt

# POC 抽樣資料
reports/mops_poc_samples/
reports/tdcc_poc_samples/

# AI CLI 評測產物（Gemini/Claude 對照樣本）
reports/claude_[0-9]*
reports/gemini_[0-9]*

# 具名真人分析報告（對可辨識個人的評價性內容；本 repo 會 push 公開 GitHub，
# 名譽權/合規風險，比照投顧 YT pipeline 不接 AI 報告的合規立場，一律不入版控）
reports/yt_analyst_*

# 日 IC parquet（可重產，較大）
reports/*.parquet

# Dashboard 測試 HTML（手動驗證用）
tools/_test_dashboard_output.html

# 外部參考 (research sample)
/remixed-*.tsx

# 外部第三方 repo（Kronos/TradingAgents 評估用，不納入版控）
third_party/

# 備份檔
*.bak_*

# M2 regression fixture（pickle + current.json 可重產，太大不納入版控）
tools/fixtures/m2_snapshot/*.pkl
tools/fixtures/m2_snapshot/*.current.json
